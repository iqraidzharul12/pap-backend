import { checkJwt } from "./checkJwt";
import { uploadId, uploadSelfie, uploadPrescription, uploadLabResult, uploadConsent, uploadNews, uploadCertificate } from "./multer";

export { checkJwt, uploadId, uploadSelfie, uploadPrescription, uploadLabResult, uploadConsent, uploadNews, uploadCertificate };
